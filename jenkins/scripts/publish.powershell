$publishDir="C:/Mutou/JenkinsPublish/webProject"
if(Test-Path $publishDir)
{
    Remove-Item $publishDir -Recurse -Force
}

md $publishDir
Compress-Archive -Path ./ -Destination $publishDir + "/dist.zip"
